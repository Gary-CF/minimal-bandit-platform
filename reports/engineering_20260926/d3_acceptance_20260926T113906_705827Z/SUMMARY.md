# D3 CLI acceptance

- stationary: 8 CSVs per run; exact replay, IDs, config, means and regret passed.
- piecewise: 8 CSVs per run; exact replay, IDs, config, means and regret passed.
- drift: 8 CSVs per run; exact replay, IDs, config, means and regret passed.

K=3, T=120, seeds=[0,1]. 24 unique episodes; each repeated once.

This is integration evidence, not a general performance claim or D6 completion.
Full repository regression tests and manual plot inspection are recorded separately.
